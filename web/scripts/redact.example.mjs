/* 脱敏词表的**结构说明**。真实词表在 redact.local.mjs，那个文件不进版本库。
 *
 *     cp web/scripts/redact.example.mjs web/scripts/redact.local.mjs
 *     然后把占位换成真实词条
 *
 * ══════════ 为什么词表要挪出版本控制 ══════════
 *
 * 2026-09-15 仓库公开后核查发现的问题：**产物是干净的，脱敏机制也在正常工作，
 * 但机制本身在公开仓库里，而且它把「真实词 → 替换词」的对照关系完整写了出来。**
 *
 * 读三行代码就能知道前雇主全称、内部系统缩写、内部平台代号，
 * 以及当时在投的目标公司叫什么 —— 后面这条尤其糟，它泄漏的不是历史，是**当下的求职意图**。
 *
 * 这不是写法问题，是结构问题：**正则必须有明文才能匹配**。
 * 把词改写、编码、哈希都会让它失效。唯一的解法是让那份明文不进版本库。
 *
 * ══════════ 什么该进 local、什么留在仓库里 ══════════
 *
 * 判据是「这个词本身会不会指认出某个具体的人/公司/系统」：
 *
 *   进 local（本文件是它的占位版）——
 *     前雇主全称、内部系统名与缩写、内部平台代号、在投目标公司及其产品名、
 *     精确到小数点的内部指标、内部项目文档编号。
 *
 *   留在 leaks.mjs 里（不必藏）——
 *     「短板」「现状自检」「你目前答不出」这类**通用的自评措辞**。
 *     它们暴露的是「我在过滤哪一类表达」，不指认任何具体对象，
 *     而且留在仓库里才能被 eval-leaks.mjs 的断言压住。
 *
 * ══════════ 同一个公司名，藏不藏取决于它以什么身份出现 ══════════
 *
 * 这一条是 2026-09-15 复核时定下来的，因为按「是不是真实公司名」一刀切会误伤：
 *
 *   要藏 —— 它是**你采集到的岗位**所属的公司，或者你的雇主。
 *           泄漏的是「你在看哪些岗位」「你在哪上班」。
 *   不藏 —— 它作为**行业例子或公开产品**被讲到（「某社区的推荐逻辑」
 *           「某大厂的 Agent 平台」）。那是公共知识，和你没有关系。
 *
 * 判据是**以什么身份出现**，不是这个词本身。所以同一个名字在两句话里
 * 可以是两种处理 —— 而这也意味着它**没法靠词表自动判**，只能人读一遍。
 *
 * ⚠️ 判完要记下来（见 `docs/脱敏核查结论_*.md` 的「明确不用动」表）。
 * 「判过、结论是不用动」和「还没查」在扫描结果里长得一模一样，
 * 不留档的话每次扫描都要重判一遍，而那个成本会让人干脆不扫。
 *
 * ══════════ 缺了这个文件会怎样 ══════════
 *
 * **不会静默跳过。** 判据是知识库在不在：
 *   · 知识库在（= 这是作者本人的机器）→ 三个消费方一律**报错退出**
 *   · 知识库不在（= 别人克隆的仓库）→ 打印「身份类断言未执行」并继续，
 *     因为那台机器上本来就没有需要脱敏的原始素材
 *
 * 这个区分是刻意的。`check-line-endings.mjs` 在单仓库合并那天踩过反面案例：
 * 目录找不到就静默返回空，于是闸门**扫了零个文件然后报告全部通过** ——
 * 一道从不报错的闸门比没有闸门更糟，因为它占着「已经检查过了」这个位置。
 */

/** @typedef {{ match: RegExp, replace: string, reason: string, hard?: boolean }} Identifier */

/**
 * 会指认出具体对象的词。**顺序有意义**：先长后短，
 * 否则短的会把长的抢先替换掉。真实词表里踩过：同一个代号有「<代号> <长产品名>」和
 * 「<代号> 平台」两种写法，短的那条排在前面时会把长的那条截成半截。
 *
 * - `match`   用于替换，必须带 /g
 * - `replace` 换成什么。选一个**看得出被脱敏过**的词，
 *             比如 `XX%` 比删掉整句好 —— 删掉会让人以为原文就没有数字
 * - `reason`  为什么它算敏感。写清楚，将来有人觉得是误判时能照着判断
 * - `hard`    true = 连图源那种「只查最硬的几个词」的场景也要查
 *
 * @type {Identifier[]}
 */
export const IDENTIFIERS = [
  {
    match: /示例公司全称有限公司/g,
    replace: "某行业平台",
    reason: "前雇主全称。这是最硬的一条",
    hard: true,
  },
  {
    match: /\bDEMOSYS\b/g,
    replace: "内部后台系统",
    reason: "内部系统缩写。三个大写字母那种，正文里很扎眼",
    hard: true,
  },
  {
    match: /Demo[- ]?AI[- ]?Center/gi,
    replace: "内部 AI 数据平台",
    reason: "内部平台代号",
    hard: true,
  },
  {
    match: /DemoTarget/gi,
    replace: "某目标公司",
    reason: "在投的目标公司名。泄漏的是当下的求职意图，不是历史",
  },
  {
    match: /DemoProduct/gi,
    replace: "某目标公司产品",
    reason: "目标公司的产品名。同上",
  },
  {
    match: /XY\s?(内容)?示例平台|XY\s?平台/g,
    replace: "内容质检平台",
    reason: "「两个大写字母代号 + 中文产品名」是内部系统命名的典型形态",
  },
  {
    match: /示例基础库/g,
    replace: "内部主数据库",
    reason: "「XX基础库」是内部系统常见命名",
  },
  {
    match: /12\.3\s?%/g,
    replace: "XX%",
    reason: "精确到小数点后一位的内部基线指标。配上上下文足够让知情的人对上号",
  },

  /* ══════════ 别漏了这一类：采集到的真实公司名 ══════════
   *
   * ⚠️ 2026-09-15 真实发生过一次回流，教训值得写在模板里：
   *
   * 当天用 `git filter-repo` 把一批真实公司名从**历史和已提交的产物**里清掉了，
   * 但没有人把它们加进这份词表。而 `modules.json` 是 `import-content.mjs`
   * 从知识库**生成**的产物 —— 知识库里那些名字还在。
   * 于是下一次重跑导入，名字原样回到了产物里，而 `leaks.mjs` 的扫描是**全绿**的，
   * 因为词表里同样没有它。
   *
   * **改写清的是「已经写下来的」，词表管的是「以后还会生成的」。**
   * 只做前者，等于给一个每次重跑都会自己长回来的东西剪了一次枝。
   *
   * 所以：凡是从招聘网站采集回来、又被写进知识库笔记的公司名，
   * 都要在这里登记。替换名建议用天干（甲乙丙…）编号，
   * 并且**和已提交产物里的叫法逐字对齐** —— 不然同一家公司会有两个代号。
   */
  {
    match: /示例公司集团|示例公司/g,
    replace: "戊公司",
    reason: "采集到的真实公司名。这一类最容易漏，因为它不在「内部系统」的直觉里",
    hard: true,
  },
];
