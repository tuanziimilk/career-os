# ---- 隐私：这些绝不能进仓库 ----
# 个人画像（含自我能力评级、弱点），只提交 config.example.py
analyzer/config.py

# 采集到的 JD 原始数据（含公司信息、招聘者姓名）
data/*
!data/.gitkeep
!data/sample_jd.txt

# 生成的分析报告（含个人缺口评级）
reports/
*.report.md
JD汇总报告.md

# API key 及任何本地配置
.env
.env.*
secrets.json

# ---- 常规 ----
__pycache__/
*.py[cod]
.venv/
venv/
node_modules/
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/

# 内容人工 review 的生成快照。它是 web/scripts/dump-content-review.mjs 的产物，
# 每跑一次就是一份带日期的新文件，而且**生成时点的内容可能还没脱敏**。
# 2026-09-15 就是这么把一份脱敏前的快照提交进公开仓库的。
# 要看就现跑：node web/scripts/dump-content-review.mjs
docs/内容review-待确认_*.md

# 脱敏词表的真实内容。正则必须有明文才能匹配，所以这份明文不能进仓库 ——
# 否则「真实词 → 替换词」的对照关系等于写在公开代码里，脱敏白做。
# 结构和判据见可公开的 web/scripts/redact.example.mjs。
web/scripts/redact.local.mjs
